#include<stdio.h>

int Addition(int X, int Y)
{
    int Result = 0;
    Result = X + Y;
    return Result;
}

int main()
{
    // extern int A;
    // extern int B;
    // register int C = 0;

    
    // C = Addition(A, B);

    // printf("The addition of %d and %d is %d.\n",A, B, C);
    
    return 0;
}